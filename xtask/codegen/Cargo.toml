[package]
name = "xtask_codegen"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
paperdown = { path = "../..", features = ["schemars"] }
